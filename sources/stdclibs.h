#ifndef _STDCLIBS_
#define _STDCLIBS_
/*
   General headers from standard C libraries
*/
#include <assert.h>
#include <stdio.h>
#include <stdlib.h>

#define MAX(A,B) ((A) > (B) ? A : B)
typedef enum { false, true } bool;
#endif